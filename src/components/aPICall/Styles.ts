import { SxProps } from "@mui/material";

export const styles = {
    listItem: {
        listStyleType: '1',
        paddingInlineStart: '0px',
        p: 2
    }

} satisfies Record<string, SxProps>